import React from 'react'

import TitleText from '../TitleText'
import styles from './styles'

const PageTitleText = props => (
  <TitleText style={styles.title} {...props} />
)

export default PageTitleText
