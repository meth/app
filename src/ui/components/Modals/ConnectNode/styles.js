import { create } from '../../../styles'

export default create({
  container: {
    flex: 0,
    padding: 20,
    backgroundColor: '$modal_content_backgroundColor',
    width: 640,
    height: 480,
    borderRadius: 10,
    zIndex: 1,
  }
})
