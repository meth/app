import { create } from '../../styles'

export default create({
  // $outline: 1,
  container: {
    backgroundColor: '#fff',
    borderWidth: 1,
    borderColor: '#fff',
    width: '200',
    height: '200'
  }
})
