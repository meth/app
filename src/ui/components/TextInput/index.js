import React from 'react'
import { TextInput } from 'react-native'

export default props => (
  <TextInput
    autoCapitalize={'none'}
    autoCorrect={false}
    autoFocus={false}
    autoFocus={false}
    {...props}
  />
)
