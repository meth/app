import { create } from '../../styles'

export default create({
  container: {
    padding: 10,
    borderRadius: 5,
    backgroundColor: '$form_wrapper_backgroundColor',
    flexDirection: 'column',
    justifyContent: 'space-around',
    alignItems: 'stretch'
  }
})
