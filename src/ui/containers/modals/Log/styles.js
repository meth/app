import { create } from '../../../styles'

export default create({
  content: {
    width: 550,
    height: 500,
    flexDirection: 'column',
    justifyContent: 'space-around',
    alignItems: 'stretch'
  }
})
