import { create } from '../../../../../styles'

export default create({
  scrollView: {
    flex: 0,
    width: '100%'
  },
  scrollViewContent: {
    flexDirection: 'row',
    flexWrap: 'nowrap',
    justifyContent: 'flex-start',
    alignItems: 'center'
  }
})
