import { create } from '../../../styles'
import commonStyles from './styles.common'

commonStyles.cards.marginTop = 5
commonStyles.tokenTable.backgroundColor = '$content_backgroundColor'
commonStyles.tokenTable.marginTop = 0

export default create(commonStyles)
