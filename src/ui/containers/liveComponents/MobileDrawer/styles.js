import { create } from '../../../styles'

export default create({
  container: {
    flex: 1,
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'space-between'
  },
  topItems: {
  },
  bottomItems: {
  }
})
