import { create } from '../../../styles'

export default create({
  container: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center'
  },
  text: {
  },
  addToAddressBookButton: {
    flex: 0,
    marginLeft: 5
  }
})
