import { create } from '../../../styles'

export default create({
  container: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center'
  },
  copyableTextContainer: {
    flex: 1
  },
  button: {
    flex: 0,
    marginLeft: 5
  }
})
