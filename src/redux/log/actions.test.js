import { LOAD_ALERTS, LOG, SEEN_ALERTS } from './actions'

describe('LOAD_ALERTS', () => {
  it('is defined', () => {
    expect(LOAD_ALERTS).toBeDefined()
  })
})

describe('LOG', () => {
  it('is defined', () => {
    expect(LOG).toBeDefined()
  })
})

describe('SEEN_ALERTS', () => {
  it('is defined', () => {
    expect(SEEN_ALERTS).toBeDefined()
  })
})
