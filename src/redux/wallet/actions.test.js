import { BALANCES, SEND_TX } from './actions'

describe('BALANCES', () => {
  it('is defined', () => {
    expect(BALANCES).toBeDefined()
  })
})

describe('SEND_TX', () => {
  it('is defined', () => {
    expect(SEND_TX).toBeDefined()
  })
})
