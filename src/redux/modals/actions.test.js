import { SHOW, HIDE } from './actions'

describe('SHOW', () => {
  it('is defined', () => {
    expect(SHOW).toBeDefined()
  })
})

describe('HIDE', () => {
  it('is defined', () => {
    expect(HIDE).toBeDefined()
  })
})
